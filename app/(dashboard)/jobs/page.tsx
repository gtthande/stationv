export default function JobCardsPage() {
  return (
    <div>
      <h2 className="text-xl font-bold">Job Cards</h2>
      <p className="mt-2 text-gray-600">
        This page lists job cards/work orders.  Include a Kanban view and list view and allow
        creation, editing, and status updates.  Each job card will link to parts and labour
        lines on separate tabs.
      </p>
    </div>
  );
}