export default function AssetsPage() {
  return (
    <div>
      <h2 className="text-xl font-bold">Rotables &amp; Tools</h2>
      <p className="mt-2 text-gray-600">
        Track rotable items and tools here.  Include status (issued/available), condition,
        calibration dates, and history logs.  Provide actions to issue and return assets.
      </p>
    </div>
  );
}